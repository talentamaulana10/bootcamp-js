function binus(name, lastName) {
    console.log('Hay ' + name + ' ' + lastName);
  function square(number) {
    return number * number;
  }
  
  binus('talenta', 'fighan!!');
  let number = square(2);
  console.log(number);